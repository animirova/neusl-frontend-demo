(function () {
    angular
        .module("NEUSL")
        .controller("homeController", homeController);

    function homeController($window, $location, $filter) {
        var model = this;

        function init() {
            model.positions

        }
        init();

    }
})();
